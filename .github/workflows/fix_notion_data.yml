name: Fix Notion Data

on:
  workflow_dispatch:
    inputs:
      mode:
        description: '操作モード（update: 既存を修正 / create: 新規作成）'
        required: true
        type: choice
        options:
          - update
          - create
      database:
        description: '対象DB（commit_log / project）'
        required: true
        type: choice
        options:
          - commit_log
          - project
      filter_property:
        description: '[updateのみ] 絞り込むプロパティ名（例：プロジェクト）'
        required: false
        type: string
      filter_value:
        description: '[updateのみ] 絞り込む値（例：古いプロジェクト名）'
        required: false
        type: string
      update_property:
        description: '[updateのみ] 更新するプロパティ名（例：プロジェクト）'
        required: false
        type: string
      update_value:
        description: '[updateのみ] 更新後の値'
        required: false
        type: string
      create_project_name:
        description: '[createのみ] 作成するプロジェクト名'
        required: false
        type: string
      create_status:
        description: '[createのみ] ステータス（未着手 / 進行中 / 完了 / 投稿済み）'
        required: false
        type: string
        default: '未着手'

jobs:
  fix-notion-data:
    runs-on: ubuntu-latest
    steps:
      - name: Install dependencies
        run: pip install requests -q

      - name: Fix Notion data
        env:
          NOTION_API_KEY: ${{ secrets.NOTION_API_KEY }}
          NOTION_DATABASE_ID: ${{ secrets.NOTION_DATABASE_ID }}
          NOTION_PROJECT_DB_ID: ${{ secrets.NOTION_PROJECT_DB_ID }}
          MODE: ${{ inputs.mode }}
          DATABASE: ${{ inputs.database }}
          FILTER_PROPERTY: ${{ inputs.filter_property }}
          FILTER_VALUE: ${{ inputs.filter_value }}
          UPDATE_PROPERTY: ${{ inputs.update_property }}
          UPDATE_VALUE: ${{ inputs.update_value }}
          CREATE_PROJECT_NAME: ${{ inputs.create_project_name }}
          CREATE_STATUS: ${{ inputs.create_status }}
        run: |
          python3 << 'PYEOF'
          import os
          import requests

          NOTION_API_KEY = os.environ["NOTION_API_KEY"]
          MODE = os.environ["MODE"]
          DATABASE = os.environ["DATABASE"]
          db_id = os.environ["NOTION_PROJECT_DB_ID"] if DATABASE == "project" else os.environ["NOTION_DATABASE_ID"]

          headers = {
              "Authorization": f"Bearer {NOTION_API_KEY}",
              "Content-Type": "application/json",
              "Notion-Version": "2022-06-28"
          }

          if MODE == "create":
              project_name = os.environ["CREATE_PROJECT_NAME"]
              status = os.environ["CREATE_STATUS"]

              if not project_name:
                  print("create_project_nameが未指定です")
                  exit(1)

              res = requests.post(
                  "https://api.notion.com/v1/pages",
                  headers=headers,
                  json={
                      "parent": {"database_id": db_id},
                      "properties": {
                          "プロジェクト名": {"title": [{"text": {"content": project_name}}]},
                          "ステータス": {"status": {"name": status}}
                      }
                  }
              )
              if res.status_code == 200:
                  print(f"作成完了: {project_name}（ステータス: {status}）")
              else:
                  print(f"作成失敗: {res.text}")
                  exit(1)

          elif MODE == "update":
              FILTER_PROPERTY = os.environ["FILTER_PROPERTY"]
              FILTER_VALUE = os.environ["FILTER_VALUE"]
              UPDATE_PROPERTY = os.environ["UPDATE_PROPERTY"]
              UPDATE_VALUE = os.environ["UPDATE_VALUE"]

              # 対象ページを全件取得
              pages = []
              cursor = None
              while True:
                  body = {
                      "filter": {"property": FILTER_PROPERTY, "rich_text": {"contains": FILTER_VALUE}},
                      "page_size": 100
                  }
                  if cursor:
                      body["start_cursor"] = cursor
                  res = requests.post(f"https://api.notion.com/v1/databases/{db_id}/query", headers=headers, json=body)
                  data = res.json()
                  pages.extend(data.get("results", []))
                  if not data.get("has_more"):
                      break
                  cursor = data.get("next_cursor")

              print(f"{len(pages)}件ヒット")

              # プロパティタイプを判定して更新
              updated = 0
              for page in pages:
                  page_id = page["id"]
                  prop = page["properties"].get(UPDATE_PROPERTY, {})
                  prop_type = prop.get("type")

                  if prop_type == "title":
                      update_body = {UPDATE_PROPERTY: {"title": [{"text": {"content": UPDATE_VALUE}}]}}
                  elif prop_type == "rich_text":
                      update_body = {UPDATE_PROPERTY: {"rich_text": [{"text": {"content": UPDATE_VALUE}}]}}
                  elif prop_type == "select":
                      update_body = {UPDATE_PROPERTY: {"select": {"name": UPDATE_VALUE}}}
                  elif prop_type == "status":
                      update_body = {UPDATE_PROPERTY: {"status": {"name": UPDATE_VALUE}}}
                  elif prop_type == "url":
                      update_body = {UPDATE_PROPERTY: {"url": UPDATE_VALUE}}
                  else:
                      print(f"未対応のプロパティタイプ: {prop_type}（page_id: {page_id}）")
                      continue

                  r = requests.patch(
                      f"https://api.notion.com/v1/pages/{page_id}",
                      headers=headers,
                      json={"properties": update_body}
                  )
                  if r.status_code == 200:
                      updated += 1
                  else:
                      print(f"更新失敗: {r.text}")

              print(f"{updated}件更新完了")

          PYEOF
